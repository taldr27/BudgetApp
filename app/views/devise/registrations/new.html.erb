<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: 'form-signin' }) do |f| %>
<nav class="navbar text-light nav-mobile p-2">
  <div class="d-flex justify-content-between w-100 align-items-center">
    <%= link_to fa_icon('arrow-left', class: 'fa-solid'), :back, class: 'btn btn-light' %>

    <a class="navbar-brand text-light" href="#">Sign Up</a>
  <div class="form-group">
    <%= f.submit "Sign up", class: "btn btn-lg btn-primary btn-block" %>
  </div>

  </div>
  </nav>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="form-group">
    <%= f.file_field :avatar %>
  </div>

  <div class="form-group">
    <%= f.text_field :name, autocomplete: "name", placeholder: 'Full name', class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.email_field :email, placeholder: 'Email', class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.password_field :password, autocomplete: "new-password", placeholder: 'Password 6 characters minimum', class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.password_field :password_confirmation, autocomplete: "new-password", placeholder: 'Confirm Password', class: "form-control" %>
  </div>


<% end %>

<%= render "devise/shared/links" %>
